@model Product
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product</title>
</head>

<body>
    @await Html.PartialAsync("_navbar") 
   @* @ 
    <h1>@ViewData["Category"]</h1>
    <h4>@(((Product)ViewData["Product"]).Name)</h4>
    <h4>@(((Product)ViewData["Product"]).Price)</h4>
    <h4>@(((Product)ViewData["Product"]).Description)</h4>
                                    *@
  @* 
    <h1>@ViewBag.Category</h1>
    <h4>@(((Product)ViewBag.Product).Name)</h1>
    <h4>@(((Product)ViewBag.Product).Price)</h1>
    <h4>@(((Product)ViewBag.Product).Description)</h1>
                                                         *@
    <h1>@ViewBag.Category</h1>
    <h4>@Model.Name</h4>
    <h4>@Model.Price</h4>
    <h4>@Model.Description</h4>

</body>

</html>